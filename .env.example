# Server Configuration
PORT=8080
LOG_LEVEL=info

# Database Configuration
# Supported drivers: postgres, mysql, sqlite
DB_DRIVER=sqlite
DB_SOURCE=ketches.db

# PostgreSQL example:
# DB_DRIVER=postgres
# DB_SOURCE=host=localhost port=5432 user=postgres password=yourpassword dbname=ketches sslmode=disable

# MySQL example:
# DB_DRIVER=mysql
# DB_SOURCE=root:yourpassword@tcp(localhost:3306)/ketches?charset=utf8mb4&parseTime=True&loc=Local

# JWT Configuration
JWT_SECRET=ketches-secret-key-change-this-in-production

# CORS Configuration
# Multiple origins separated by comma
# Examples:
#   Development (allow localhost):
#     CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
#   
#   Production (specific domains):
#     CORS_ALLOWED_ORIGINS=https://app.example.com,https://admin.example.com
#   
#   Wildcard subdomain:
#     CORS_ALLOWED_ORIGINS=https://*.example.com
#   
#   Development only (allow all - NOT recommended for production):
#     CORS_ALLOWED_ORIGINS=*
#
# If not set, defaults to common development ports
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173
